import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { compressUpload } from "@/lib/imageCompression";
import { signInMember, signUpApprovedMember } from "@/lib/supabase";
import { Check, ImagePlus, LogIn, UserPlus } from "lucide-react";

export default function MemberAuthDialog() {
  const [mode, setMode] = useState<"login" | "signup">("login");
  const [open, setOpen] = useState(false);
  const [busy, setBusy] = useState(false);
  const [message, setMessage] = useState("");
  const [form, setForm] = useState({ email: "", password: "", fullName: "", phone: "", memberId: "", photoUrl: "" });

  const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const onPhoto = async (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file) return; const compressed = await compressUpload(file, "members"); update("photoUrl", URL.createObjectURL(compressed)); setMessage("ছবি সংকুচিত করে প্রস্তুত করা হয়েছে"); };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setMessage(""); try { if (mode === "login") { await signInMember(form.email, form.password); setMessage("লগইন সফল হয়েছে"); } else { if (form.fullName.trim().length < 3 || form.phone.trim().length < 8 || form.memberId.trim().length < 3) throw new Error("নাম, ফোন ও সদস্য আইডি সঠিকভাবে দিন"); await signUpApprovedMember(form); setMessage("সাইনআপ সফল। ইমেইল যাচাই করে প্রবেশ করুন"); } } catch (error) { setMessage(error instanceof Error ? error.message : "অনুরোধটি সম্পন্ন করা যায়নি"); } finally { setBusy(false); } };

  return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button className="member-auth-trigger" size="sm" variant="outline"><LogIn className="mr-2 h-4 w-4" />সদস্য প্রবেশ</Button></DialogTrigger><DialogContent className="member-auth-dialog max-w-md max-h-[90vh] overflow-y-auto" dir="rtl"><DialogHeader><DialogTitle className="font-[Noto_Sans_Bengali] text-xl text-[#122b3e]">{mode === "login" ? "সদস্য / এডমিন লগইন" : "এডমিন অনুমোদিত সদস্য সাইনআপ"}</DialogTitle></DialogHeader><div className="mb-3 grid grid-cols-2 rounded-lg bg-slate-100 p-1"><button className={`rounded-md px-3 py-2 text-sm ${mode === "login" ? "bg-white font-bold shadow-sm" : "text-slate-500"}`} onClick={() => setMode("login")}>লগইন</button><button className={`rounded-md px-3 py-2 text-sm ${mode === "signup" ? "bg-white font-bold shadow-sm" : "text-slate-500"}`} onClick={() => setMode("signup")}>সাইনআপ</button></div><form className="grid gap-4" onSubmit={submit}>{mode === "signup" && <><div><Label>পূর্ণ নাম</Label><Input required value={form.fullName} onChange={(e) => update("fullName", e.target.value)} placeholder="আপনার পূর্ণ নাম" /></div><div className="grid grid-cols-2 gap-3"><div><Label>সদস্য আইডি</Label><Input required value={form.memberId} onChange={(e) => update("memberId", e.target.value)} placeholder="S-001" /></div><div><Label>মোবাইল নম্বর</Label><Input required value={form.phone} onChange={(e) => update("phone", e.target.value)} placeholder="০১XXXXXXXXX" /></div></div><div className="flex items-center gap-3 rounded-lg border border-dashed border-emerald-200 bg-emerald-50 p-3"><div className="grid h-12 w-12 place-items-center overflow-hidden rounded-full bg-white text-emerald-700">{form.photoUrl ? <img src={form.photoUrl} alt="সদস্যের ছবি" className="h-full w-full object-cover" /> : <ImagePlus className="h-5 w-5" />}</div><div className="min-w-0 flex-1"><strong className="block text-xs">প্রোফাইল ছবি</strong><span className="block text-[10px] text-slate-500">ছবি KB আকারে সংকুচিত হবে</span></div><label className="cursor-pointer rounded-md bg-white px-2 py-1 text-xs font-bold text-emerald-700">ছবি দিন<input className="hidden" type="file" accept="image/*" onChange={onPhoto} /></label></div></>}<div><Label>ইমেইল</Label><Input required type="email" value={form.email} onChange={(e) => update("email", e.target.value)} placeholder="আপনার অনুমোদিত ইমেইল" /></div><div><Label>পাসওয়ার্ড</Label><Input required minLength={6} type="password" value={form.password} onChange={(e) => update("password", e.target.value)} placeholder="কমপক্ষে ৬ অক্ষর" /></div>{message && <p className={`rounded-md p-3 text-xs ${message.includes("সফল") || message.includes("প্রস্তুত") ? "bg-emerald-50 text-emerald-700" : "bg-rose-50 text-rose-700"}`}>{message}</p>}<Button disabled={busy} className="w-full bg-[#092337]" type="submit">{busy ? "অপেক্ষা করুন…" : mode === "login" ? <><LogIn className="mr-2 h-4 w-4" />নিরাপদে লগইন</> : <><UserPlus className="mr-2 h-4 w-4" />সাইনআপের অনুরোধ পাঠান</>}</Button><p className="text-center text-[11px] leading-5 text-slate-500">শুধু এডমিন অনুমোদিত ইমেইল ও সদস্য আইডি দিয়েই সাইনআপ করা যাবে। এডমিনও একই লগইন ফর্ম ব্যবহার করবেন; ব্যবহারকারীর অনুমতি প্রোফাইল থেকেই নির্ধারিত হবে।</p></form></DialogContent></Dialog>;
}
